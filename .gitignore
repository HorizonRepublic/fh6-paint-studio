# ── Binaries & build output ─────────────────────────────────────────────────
/bin/
/bin-temp/
/release/
/dist/
# Portable Vulkan toolchain (glslang + headers + volk + VMA), fetched locally, not published
/third_party/vulkan/
cmd/studio/rsrc_windows_amd64.syso
*.exe
*.exe~
*.dll
*.so
*.dylib
*.lib
*.exp
*.spv

# ── Go ──────────────────────────────────────────────────────────────────────
# Test binary built with `go test -c`, plus coverage & profiling output
*.test
*.out
*.prof
coverage.*
# Go workspace files (per-machine)
go.work
go.work.sum
# Environment files
.env
.env.*
!.env.example

# ── Logs ────────────────────────────────────────────────────────────────────
*.log

# ── Project working directories (local only, not published) ─────────────────
/debug/
/_reference/
/docs/
/checkpoints/
/out/
/milestones/

# ── Editor & tooling state (local, not published) ───────────────────────────
.idea/
.vscode/
*.code-workspace
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~
CLAUDE.md
AGENTS.md
GEMINI.md
.claude/
.serena/
.cursor/
.cursorrules
.cursorignore
.aider*
.gemini/
.codeium/
.windsurf/
.continue/
.github/copilot-instructions.md

# ── OS cruft ─────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
Thumbs.db
ehthumbs.db
desktop.ini

# Frame bench-bank: 435 MB of stills sliced from local video files. Local measurement asset,
# never committed (same rule as the vinyl catalog).
docs/frame-bank/

# Proposer training scratch: pretrained backbones, checkpoints and LoRA ensembles pulled down by the
# training scripts. 3 GB of third-party weights — a local training asset, never committed. The
# SHIPPED proposer is internal/aimodel/proposer.bin (2.7 MB), which is a different thing entirely.
/models/

# Generated by scripts/build-vulkan.ps1 from the release version.
internal/backend/vulkan/shim/fh6vk_version.h

# Local test drops from scripts/build-client-release.ps1 dry runs.
/release-test*/
